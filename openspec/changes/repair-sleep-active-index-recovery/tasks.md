## 1. FlowGuard Ownership And Model-Miss Closure

- [x] 1.1 Run the real FlowGuard engine/version/project audit and freeze the product-runtime commitment, owner models, observed timeout evidence, and OpenSpec read-only context identity.
- [x] 1.2 Extend the existing Sleep/lifecycle models for staged candidate transitions, bounded replay, partial-timeout recovery, and one final index owner; do not add a parallel authority model.
- [x] 1.3 Add Model Miss evidence for `state_too_coarse` and `evidence_overclaimed`, including observed failure, same-class transition family, owner code contracts, counterexample, and required revalidation.
- [x] 1.4 Update DevelopmentProcessFlow/adoption evidence for peer writes, focused parallel checks, unique foreground aggregate owners, install/runtime/Git/release freshness, and timeout cleanup gates.

## 2. Scale-Bounded Lifecycle Implementation

- [x] 2.1 Refactor candidate creation/reuse to stage ordered snapshot and outcome events against the cycle lifecycle snapshot without per-candidate publication or repeated projection loads.
- [x] 2.2 Refactor candidate lifecycle review to stage reopening, promotion, downgrade, parking, and calibration events while preserving per-entry causal order and exact decision identities.
- [x] 2.3 Commit staged observation, candidate, and review events through bounded atomic lifecycle batches with exact requested/created/reused/residual/replay accounting and no per-event fallback.
- [x] 2.4 Bind committed/reused event ids back into Sleep dispositions, lifecycle-review decisions, backlog accounting, and the final native receipt.

## 3. Active-Index Publisher Authority

- [x] 3.1 Require an explicit active-index publisher identity and reject missing or unauthorized rebuild requests before any state write.
- [x] 3.2 Pass the canonical Sleep publisher through model publication and final no-delta index ownership; pass the versioned migration publisher through every migration rebuild call site.
- [x] 3.3 Preserve marker-token, superseding-publication, source, lifecycle, LogicGuard, full-validation, fast-validation, authority-write, and marker-removal ordering.
- [x] 3.4 Exhaust direct rebuild call sites and update tests/tools to use an authorized identity only when they intentionally own publication.

## 4. Regression Evidence

- [x] 4.1 Add candidate create/park and reopen/promote ordered-batch tests with stable ids and terminal replay parity.
- [x] 4.2 Add large-ledger replay-count and partial-timeout retry-convergence tests that prove bounded work and zero duplicate events/candidates.
- [x] 4.3 Add authorized/unauthorized rebuild, marker-token race, failed activation, and exact marker-clearing tests.
- [x] 4.4 Add fail-closed preflight tests for retrieval, Dream, organization contribution, and organization maintenance, with downstream stages explicitly not run.
- [x] 4.5 Update current-runtime, installer, automation receipt, and FlowGuard model/test evidence for the changed contracts.

## 5. Focused And Aggregate Validation

- [x] 5.1 Validate the OpenSpec change and run affected lifecycle, Sleep, retrieval, Dream, organization, migration, installer, and automation tests; repair every in-scope failure.
- [x] 5.2 Run the affected FlowGuard models, conformance/replay/known-bad checks, Model Miss closure, model-test alignment, and required parent/mesh reattachment.
- [x] 5.3 If any maintained Skill source changes, run its target-native checks and close the exact single-member SkillGuard maintenance unit; otherwise record SkillGuard as unaffected.
- [x] 5.4 Freeze source, toolchain, check inventory, dependencies, and peer writes; execute the unique foreground all-model owner and full-test owner once.

## 6. Installation And Runtime Recovery

- [x] 6.1 Pause the four repository-managed automations while preserving their exact schedules, prompts, model policy, and user state for restoration.
- [x] 6.2 Run the installer and independent install check; require current global skill/default blocks, managed skills, automations, migration, active-index, and `strong_session_defaults` evidence.
- [x] 6.3 Execute exactly one canonical Sleep wrapper recovery run and apply the no-retry/zero-descendant rule on any timeout or interruption.
- [x] 6.4 Verify completed Sleep receipt, advanced-or-current watermark, absent invalidation marker, exact index/authority/LogicGuard/lifecycle bindings, full/fast validation, and released locks.
- [x] 6.5 Run one explicit normal retrieval check, then execute Dream, organization contribution, and organization maintenance sequentially with new run ids and complete terminal accounting.
- [x] 6.6 Restore and verify the four automations as `ACTIVE` with their intended local schedules.

## 7. OpenSpec And Release Closure

- [ ] 7.1 Verify implementation against proposal/design/specs/tasks, sync delta specs, and archive `repair-sleep-active-index-recovery` only after runtime recovery evidence is current.
- [x] 7.2 Perform the repository release audit across VERSION, README, local/remote Git, tags, GitHub Release state, latest-tag target, and release-worthy delta.
- [ ] 7.3 Select the next version, update version/release notes, create the release commit, then create and verify the tag target.
- [ ] 7.4 Push the branch and tag, create the GitHub Release, and verify remote branch/tag/release identities and required workflow evidence.
- [ ] 7.5 Record the structured KB postflight observation, reconcile final Git/install/automation/process/receipt state, and close the continuous goal only when every required stage is terminal.
