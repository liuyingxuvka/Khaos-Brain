## 1. Freeze the upgrade boundary

- [x] 1.1 Record the concurrent-work baseline, managed automation states, active-lane status, KB counts, history size, event watermark, installed Skill fingerprints, and current Guard evidence without modifying peer-owned files.
- [x] 1.2 Keep all five retained automations—Sleep, Dream, organization contribution, organization maintenance, and system update—paused for the migration; preserve each status plus independent `user_paused` value, and pause/delete exact legacy Architect surfaces instead of restoring them.
- [ ] 1.3 Re-run predictive-KB recall at each material phase change and record the final mistake-first KB postflight observation when the upgrade reveals reusable maintenance lessons.

## 2. Establish executable ownership and assurance

- [x] 2.1 Add the FlowGuard lifecycle child model for observation disposition, candidate terminal outcomes, Sleep watermark commit, Dream evidence closure, and active-index eligibility.
- [x] 2.2 Add the FlowGuard migration child model for ordered checkpoints, pause/restore behavior, Architect tombstones, interruption recovery, idempotency, and rollback.
- [x] 2.3 Add known-bad variants for premature watermark advance, repeated no-delta Dream writes, candidate-source collapse, recursive assurance, prune-before-archive, early automation restore, and residual Architect assets, and prove that the real FlowGuard engine rejects them.
- [x] 2.4 Add the field-lifecycle inventory for new lifecycle/migration fields, retrieval source boundary, assurance invocation context, and every removed or renamed Architect field, queue, route, handoff, gate, Skill, and automation id.
- [x] 2.5 Add finite contract-exhaustion coverage for fresh install, legacy active/paused Architect, missing manifest, interrupted/repeated migration, stale index, failed validation, no-delta Dream, local/organization candidate source, and outer/child assurance combinations.
- [ ] 2.6 Add executable model-to-code-to-test alignment and a background regression suite receipt with current-evidence freshness checks.
- [x] 2.7 Give all five retained background automations target-specific current obligations, immutable per-run native-output artifacts, an exact declared-check inventory, AST-resolved per-test JUnit capability evidence, and target-owned positive/shallow fixtures; keep version capability regressions separate from scheduled-run completion. For a prepared system update, make the first `authorize` stage emit a non-terminal declared-check receipt with `overall_complete=false` and no closure, then require the sole `enforced` closure in a fresh composed `authorize+finalize` run; the three legal no-op branches are also enforced terminal completions.
- [x] 2.8 For every retained task, declare one exact current check inventory covering intake, native execution, terminal validation, target-owned positive/shallow fixtures, and any conditional branch/finalization check; require enrolled current provider/runtime capabilities for declared-check inventory, reconciliation, installation binding/currentness, and single-flight execution. Reject missing or repeated checks, generic fixtures, stale runtimes, caller-authored pass flags, and proposal-only evidence.
- [x] 2.9 Derive every phase-depth item and contribution range from the exact validated per-obligation native receipt evidence; require the installed builder's six-field current installation identity and exact same-run `stage_depth -> target terminal -> close` depth-receipt reuse without target-check reruns; reject caller-authored identity/terminal fields, catalog-only phase expansion, generic `obligation:one/two` calibration, Dream contention-as-success, Sleep without exact shared-lock ownership/release, real-vs-fixture receipt schema drift, generic all-obligation no-op expansion, update no-op authorization-only completion, and any `already-upgrading`, `failed-awaiting-user`, or `concurrent-update` blocker mislabeled as a successful no-op.
- [x] 2.10 After all five current contracts pass target-owned positive and shallow fixture checks, remove the exact former work contract, underscore manifest, flat run records, and empty former runtime directories; keep no compatibility, conversion, renewal, retirement-receipt, alias, or fallback surface; make fresh install and every upgrade block any reintroduced residual.
- [x] 2.11 Backpropagate the live Sleep timeout miss into FlowGuard: reject per-observation lifecycle replay, retention of a fresh lock whose recorded PID is dead, validation timeouts that leave descendants, and parent timeouts without child-cleanup margin.
- [x] 2.12 Register the current-only retrieval, organization layout, automation runtime, launcher grammar, and direct upgrade commitments in the canonical FlowGuard behavior ledger; add field-lifecycle dispositions and known-bad cases for every removed compatibility/fallback surface.
- [x] 2.13 Backpropagate the post-green organization-layout miss into the current commitment: register reader, outbox, adoption, maintenance, and GitHub-check consumers; add the 4-by-3 same-class contract family; repair the stale installer owner binding; and close the analogous consumer scan with current tests.
- [x] 2.14 Backpropagate the formal-install misses: treat installed Skills as verified deployment projections and supervise them through a repository-local content-addressed copy of exact installed bytes with target-owned run state instead of direct outside-root execution, recompilation, or source substitution; canonicalize obsolete update state before any current-only aggregate consumer with exact rollback; and derive organization maintenance action ids from the complete canonical decision payload with exact count/uniqueness checks.
- [x] 2.15 Backpropagate the installed-supervision misses: project the frozen SkillGuard behavior plus current global-router sibling at one short repository-local content-addressed root, exclude runtime state/caches/bytecode, suppress bytecode writes in the executor and child Python processes so repeated imports cannot mutate the projection, require exact verified installed-runtime fingerprint equality, keep both control projections independent of deep Windows run paths, and bind every target-owned positive/shallow fixture check exactly once across the manifest, native-check inventory, and provider readiness inventory.
- [x] 2.16 Backpropagate the aggregate-alignment miss: bind every model-test obligation only to a declared logical validation owner, treat raw receipt names and unknown owners as explicit failed evidence instead of dictionary exceptions, and cover both current receipt projection and failed-owner behavior in the contract mesh.
- [x] 2.17 Backpropagate the scheduled-supervision authority miss: derive source-vs-installed execution only from the exact canonical source root or exact active installed root, keep the surface label display-only, block unknown or ambiguous roots, and cover the scheduled-label, misleading-label, and unknown-root cases in FlowGuard, the behavior ledger, static current-only checks, and focused tests.
- [x] 2.18 Backpropagate the formal-install lifecycle-writer miss: replace ownerless directory existence with the sole current PID/thread/token identity, recover dead or interrupted owners without stealing a live owner, support active-owner-thread reentrancy, make release failure visible, and reject orphan retention, self-deadlock, and hidden release failure in FlowGuard and focused tests.
- [x] 2.19 Backpropagate real-corpus Sleep convergence evidence: reuse the exact already-validated generation catalog for lifecycle review, skip semantic re-review of parked entries whose evidence fingerprint is unchanged, and keep a new evidence digest as the sole reopen trigger.
- [x] 2.18 Backpropagate the branch-disposition miss: distinguish `performed`, proof-bound `not_applicable`, and failed evidence; require every not-applicable claim to bind one evaluated false applicability gate, exact run id, reason, terminal state, non-mutation proof, canonical gate-facts hash, and exact source projection; reject forged proofs, missing branch fields, and generic shared passes.
- [x] 2.19 Backpropagate conditional finalization into the single current contract: prepared authorization emits a non-terminal declared-check receipt and no closure; `finalization-runtime` remains a target-owned conditional check; legal no-op or a fresh composed finalization consumes the exact current receipt in the sole `enforced` closure without rerunning target checks.
- [x] 2.20 Backpropagate the long-native supervision race: create one official persistent supervision session before native execution, freeze and retain the exact installed SkillGuard context/runtime plus target control and six-field identity, reuse that same authority after native execution, suppress projection cache mutation, and prove that target Skill changes do not require global SkillGuard reinstallation while a newer live global identity affects only the next run.
- [x] 2.21 Backpropagate the real Dream `receipt-path-missing` model miss: separate start-frozen static authority from post-native dynamic evidence, project only the exact seven declared run-evidence keys into the retained supervisor, clear missing inherited values, reject undeclared keys, register the repaired behavior owner, and cover missing-projection plus non-isolation known-bad variants.
- [x] 2.22 Backpropagate the aggregate resource-contention miss: give real scheduled production an exclusive aggregate lane after ordinary child checks, give each Sleep cycle one final active-index publication/validation owner, reuse its current receipt when no later eligibility mutation occurs, and reject duplicate final index validation plus scheduled-production competition known-bad variants.
- [x] 2.23 Backpropagate real no-delta Sleep evidence: prohibit a second full model publication, let one final owner validate or directly rebuild only the active index, persist one same-state calibration watermark when parked evidence changes without reopening, and keep calibration-only metadata outside retrieval-eligibility freshness.
- [x] 2.24 Backpropagate the interrupted Dream-handoff model miss: defer handoff acknowledgement until candidate/model publication commits, reopen only the exact dead owner's uncommitted acknowledgements, and reject acknowledgement-before-publication in FlowGuard and focused tests.
- [x] 2.25 Backpropagate the real multi-handoff timeout miss: compile every pending Dream handoff admission/disposition into the same bounded atomic lifecycle batch, reject per-handoff full-ledger replay in FlowGuard, and cover four-handoff batching in focused tests.
- [x] 2.26 Backpropagate the secondary real Sleep timeout miss: keep ordered lifecycle idempotency output while using an in-memory set for linear duplicate detection, reject quadratic single-replay cost in FlowGuard, and cover scaling plus the 73,680-event real authority replay.

## 3. Make experience lifecycle explicit and retrieval-safe

- [x] 3.1 Implement a versioned disposition ledger with stable observation/candidate identities, evidence grades, provenance, decision owner/time/reason, terminal outcomes, parked reopen conditions, and deterministic replay.
- [x] 3.2 Admit every new observation into the lifecycle without hiding failures, and guarantee a committed disposition by the next successful Sleep pass while keeping trusted promotion evidence-dependent.
- [x] 3.3 Implement bounded candidate outcomes (`trusted`, `merged`, `superseded`, `rejected`, or `parked`) plus evidence-driven reopen, promotion, demotion, and merge behavior.
- [x] 3.4 Build and atomically replace a compact active-knowledge index from lifecycle truth; normal retrieval fails visibly when that sole current index is absent or stale.
- [x] 3.5 Exclude rejected, merged, superseded, retired, and parked entries from all retrieval paths, bind local candidate ranking by evidence grade, and keep read-only organization-candidate visibility explicitly untrusted and outside the local active index.
- [x] 3.6 Record real-task outcome receipts, user corrections, verification results, misleading hits, and no-card cases; add reproducible Top-3 safety, calibration, and latency evaluation.
- [x] 3.7 Separate compact foreground authority validation from full maintenance audit: observation-only intake stays current, entry transitions invalidate before mutation, active source drift fails closed, and only a validated rebuild reactivates indexed reads.
- [x] 3.8 Remove filtered-scan and pre-standard retrieval success paths so missing or stale current authority returns an explicit unavailable result and never scans another source.

## 4. Make Sleep incremental and Dream convergent

- [x] 4.1 Add a durable Sleep watermark that consumes new events plus explicitly invalidated older items and advances only after dispositions, index validation, and a success receipt commit.
- [x] 4.2 Add a bounded legacy-debt queue, evidence-prioritized processing, per-pass backlog metrics, and safe retry behavior that cannot lose skipped work.
- [x] 4.3 Add Dream evidence fingerprints keyed by route, mode, source ids, and evidence fingerprint so an unchanged passed, failed, weak, or inconclusive experiment becomes a deterministic no-op.
- [x] 4.4 Permit Dream reopen only after a material evidence or disposition delta, and route every knowledge-changing Dream result exclusively back to Sleep.
- [x] 4.5 Remove Architect queue/handoff ownership from Sleep, Dream, prompts, receipts, and completion gates while preserving inert historical provenance.
- [x] 4.6 Recover an interrupted Sleep whose lifecycle dispositions committed before its watermark: skip already-terminal history without readmission or redisposition, batch genuinely pending admission/disposition events into one replay-before/replay-after transaction, and expose batch/skip counts in the receipt.
- [x] 4.7 Build one calibration evidence index per Sleep cycle and reuse it across all candidate lifecycle reviews; never reload the complete outcome and lifecycle authorities once per candidate.

## 5. Retire Architect and make installation transactional

- [x] 5.1 Define canonical retired-id tombstones for automation `kb-architect` and Skill `kb-architect-pass`, and remove Architect from repository-managed Skills, prompts, launchers, lanes, route registries, docs, update gates, and readiness dependencies.
- [x] 5.2 Make fresh installation omit Architect and make every supported legacy upgrade remove only exact managed Architect artifacts regardless of prior active/paused state or manifest completeness.
- [x] 5.3 Replace `architect_update_check` with a neutral system-update gate and migrate legacy Architect handoff records to Sleep evidence or system-maintenance observations.
- [x] 5.4 Stage complete Skill and automation trees transactionally, compile and validate canonical SkillGuard contracts before activation, compare complete source/staged/installed manifests, block downgrade or concurrent drift, and retain rollback copies until post-activation validation passes. Journal every upgrade attempt separately from the last-known-good install manifest.
- [x] 5.4a Compare current SkillGuard authority by obligation/evidence/mandatory-owner coverage rather than check-id, native-route, or depth-dimension subset monotonicity; allow coverage-preserving check reorganization and proof-bound conditional depth-wrapper moves to unchanged independent hard owners; block any semantic loss with positive and negative regression evidence.
- [x] 5.5 Preserve status and independent user-pause state for every surviving managed automation across install/upgrade, keep all live survivors paused until the aggregate and final SkillGuard gates pass, authorize a source/target-hash-bound staged restoration plan, then apply/read back only those hashes; prevent aggregate child tests from recursively re-entering real upgrade gates.
- [x] 5.6 Add machine-readable Architect-absence, SkillGuard parity, installation health, interruption recovery, migration-receipt, router-refresh-receipt, and live registry/prompt freshness checks. Freeze complete SkillGuard, FlowGuard, and LogicGuard validation-toolchain snapshots before long assurance, make every child consume all three exact digests, and recheck all three live identities before final commit. Refresh the router once before assurance and again after the final managed Skill-tree write; any later failure must preserve a retryable PAUSED checkpoint rather than being ignored after commit.
- [x] 5.7 Resolve Architect routing only from the current Codex home's active registry, prove an unrelated stale external registry cannot block retirement, and fail closed when the active registry is unreadable or still contains Architect.
- [x] 5.8 Give every long validation owner descendant-tree cleanup on timeout, prove the captured tree reaches zero, and order native, scheduled-production, aggregate, and installer timeouts with explicit cleanup margins.
- [x] 5.9 Remove fixed automation model/reasoning defaults, launcher argument aliases and implicit subcommands, and scheduled-runtime repair of obsolete update failures; resolve them only from current facts or the one-time upgrade phase.

## 6. Settle historical knowledge and storage debt

- [x] 6.1 Implement the versioned maintenance/history schema and journal state machine `preflight -> snapshot -> classify -> canonicalize-runtime -> settle-logical-debt -> archive-cold-evidence -> prune-derived-data -> rebuild-index -> validate -> committed`, with `paused_failed`, checkpoints, fingerprints, receipts, resume, rollback, and writer exclusion.
- [x] 6.2 Inventory canonical evidence, compact cold evidence, and disposable derivations with ownership, counts, bytes, hashes, dependencies, and prune eligibility before any destructive step.
- [x] 6.3 Settle unresolved legacy observations and candidates into explicit lifecycle outcomes; make residual hard debt block completion and make parked reopen conditions executable.
- [x] 6.4 Compact retired Architect and old maintenance provenance into integrity-checked cold evidence without leaving active routes, queues, or retrieval exposure.
- [x] 6.5 Deduplicate redundant snapshots and remove only receipt-covered, regenerable caches, sandboxes, test output, completed maintenance workspaces, and other declared Chaos Brain derivations after archive and rollback checks pass.
- [x] 6.6 Rebuild the active index atomically, publish before/after file/byte and debt accounting, and prove repeat and interrupted migrations converge without duplicate work or receipts.
- [x] 6.7 Prove large-history settlement uses bounded atomic lifecycle batches with at most one replay before and after each batch, and that a partially completed legacy per-item attempt resumes without duplicate events or candidates.
- [x] 6.8 Handle verified Windows read-only managed artifacts without weakening ACL or ownership boundaries, and merge partial prune manifests so interrupted deletion accounting remains exact.
- [x] 6.9 Move resolved migration failures into diagnostic history and reject any committed journal that still reports an active failure.
- [x] 6.10 Detect managed files reintroduced during validation or after commit, reopen the gate, and converge them through resumable receipt-backed reconciliation passes.
- [x] 6.11 Make inventory, hashing, archive, prune, residual checks, and directory cleanup safe for Windows extended-length managed paths.
- [x] 6.12 Settle observations admitted by concurrent AI work during or after migration through bounded post-commit logical reconciliation receipts.
- [x] 6.13 Add the direct current-format canonicalization phase: migrate organization schema/layout, settle exact obsolete update state, remove old runtime readers/keys/roots, publish residual-zero receipts, and make repeat upgrades verification-only.

## 7. Update the product contract and operator guidance

- [x] 7.1 Update PROJECT_SPEC, README, AGENTS defaults, install/update runbooks, maintenance prompts, and release migration guidance to describe the automatic AI-only lifecycle, incremental Sleep, convergent Dream, Architect retirement, and upgrade debt cleanup.
- [x] 7.2 Update the installer manifest/checklist so another computer inherits canonical Skills, surviving automations, phase-change recall, mistake-first postflight, retirement tombstones, migration versions, and aggregate readiness checks.
- [x] 7.3 Ensure all machine interfaces and receipts remain canonical, encoding-stable JSON while Chinese wording stays in human-facing projections.
- [x] 7.4 Document and install the zero-compatibility/zero-fallback rule: old managed formats are upgrade input only, current-authority absence is visible failure, and no daily compatibility surface is supported.

## 8. Verify in fixtures and then migrate this machine

- [x] 8.1 Add focused lifecycle, retrieval/source-boundary, Sleep/Dream, history-migration, cross-version upgrade, installation/non-recursion, and update-gate tests using the production code paths.
- [x] 8.2 Run the shadow-home fixture matrix for fresh, legacy active, legacy paused, missing-manifest, partial, interrupted, failed-validation, and repeated upgrades; repair every failure.
- [x] 8.2a Add focused positive and exact rejection tests for current-index-only retrieval, schema-v1 organization migration, old-root residual rejection, missing provider facts, removed launcher grammar, and upgrade-only update-state settlement.
- [x] 8.2b Prove every normal organization consumer blocks old-root residuals, while adopted cards atomically invalidate and rebuild the sole local active index instead of relying on scan fallback.
- [ ] 8.3 Run FlowGuard, FieldLifecycleMesh, ContractExhaustionMesh, Model-Test Alignment, TestMesh, SkillGuard, install-health, retrieval-quality, and full repository regressions in the background with durable final receipts; repair every failed or stale gate.
- [ ] 8.4 Run a final phase-change KB preflight, execute the production versioned migration and transactional installer on this computer, and keep all five retained automations paused until every live migration, installation, model, and SkillGuard gate passes.
- [ ] 8.5 Verify zero residual Architect assets or active handoffs, zero residual hard lifecycle debt, valid cold-archive hashes, current index eligibility, source/install parity, storage accounting, and committed install/migration receipts.
- [ ] 8.6 Execute the official SkillGuard supervisor for all five source and installed automation contracts, require five current declared-check receipts consumed by five sole `enforced` closures, require system-update finalization to consume both exact native and staged-restoration artifacts, and prove partial/native-only receipts are blocked before enabling any scheduled task.
- [ ] 8.7 Only after 8.6 passes, complete the hash-bound state transaction for Sleep, Dream, organization contribution, organization maintenance, and system update, prove Architect remains absent, then apply this user's latest explicit current-machine override by keeping all five `PAUSED` with `user_paused=true` and reading back the whole group again; any activation, pause-bit mismatch, readback failure, or final-check failure remains a blocker. Keep this local all-paused override separate from the portable installer rule that preserves each machine's prior user-pause choice.

## 9. Close the change only on current evidence

- [ ] 9.1 Run strict OpenSpec validation and exactly one aggregate final-readiness execution after all implementation and current-machine changes settle; require focused, semantic, FlowGuard, SkillGuard, installation, retrieval, and full-regression claims to consume that aggregate evidence graph instead of launching overlapping commands again.
- [ ] 9.2 Complete the explicit KB postflight, record any reusable lesson as structured candidate feedback rather than rewriting trusted cards inline, and confirm no peer-owned work was lost.
- [ ] 9.3 Mark every implementation and verification task complete only with current receipts, run `openspec verify`, and establish archive-ready status. Treat `openspec archive` as the post-task closure action, then close the long-running goal only after the archive succeeds and no required work remains.
